# DESIGN.md — Город трафика × Трафик Лаб

> Единственный источник правды по визуалу. Читать ПЕРЕД тем, как писать UI.
> Мир построен 22.08.2026 по четырём референсам заказчика. Значения цвета сняты
> пиксельным замером с этих референсов, а не подобраны на глаз.

---

## 1. DESIGN READ

**Что это:** Telegram Mini App — кинематографичная воронка для перформанс-маркетологов.
Человек летит над ночным Городом трафика, попадает в подпольную лабораторию гипотез,
собирает связку и покупает практикум.

**Язык мира:** дорогое ночное кино. Глубокая сине-чёрная ночь, по которой текут
световые трассы, и один тёплый источник света — жёлтый. Ничего пастельного,
ничего «курсового», ни одного корпоративного градиента.

**Тема:** тёмная, всегда. Светлого режима нет: сцена — ночь.
**Дилы:** `DESIGN_VARIANCE: 8` · `MOTION_INTENSITY: 8` · `VISUAL_DENSITY: 4`

### Два акта — одна система

| Акт | `data-act` | Ground | Тёплый акцент | Холодный акцент | Где |
|---|---|---|---|---|---|
| Город | `city` | сине-чёрный `#050D1B` | золото `#F9B706` | электрик `#3E82F7` | экраны 1, 7 |
| Лаборатория | `lab` | тёпло-чёрный `#0B0A06` | тот же жёлтый `#F9B706` | кислота `#C8F53C` | экраны 2–6, 8, 9 |

Акт меняет **ground и холодный акцент**, но не типографику, не сетку, не радиусы
и не кнопки. Жёлтый общий: это фирменный цвет костюма на референсе, он сшивает
город и лабораторию в один мир.

### Референсы заказчика

| Файл | Что берём |
|---|---|
| Ночной город с районами | Палитру ночи, световые трассы, вертикальный кадр |
| Развязка «ГОРОД ТРАФИКА» | Композицию «дороги сходятся вверх», арку-вывеску |
| Район конверсий на холме | Идею недоступной высокой цели, золотую дорогу к ней |
| Персонаж в жёлтом костюме | Жёлтый `#F9B706`, кислотную колбу, эстетику промзоны |

### Анти-референсы

Светлые курсовые лендинги · фиолетовый AI-градиент · glassmorphism как украшение ·
стоковые иллюстрации с плоскими человечками · неоновый киберпанк с розовым ·
инфографика со стрелочками · «геймификация» с бейджами и конфетти.

---

## 2. ЗАПРЕТЫ (жёсткие)

1. **Нет светлой темы.** Ground всегда тёмный. Белых полотен в макете не существует.
2. **Нет градиентного текста.** Акцент даёт вес и размер, а не заливка букв.
3. **Нет стекла ради красоты.** `backdrop-filter` только там, где под слоем реально
   что-то есть и его надо приглушить: панель поверх фото города.
4. **Нет карточек-плиток «иконка + заголовок + текст» как структуры экрана.**
   Экран несёт одно действие, а не сетку одинаковых плашек.
5. **Нет надзаголовков (kicker/eyebrow).** Заголовок несёт себя сам.
6. **Нет эмодзи.** Иконки — Phosphor, `weight="regular"`, один вес по всему приложению.
7. **Моноширинный — только для данных:** таймкоды, счётчик жизней, номера ассистентов,
   цена. Не для «технологичности».
8. **Нет `h-screen`.** Только `min-h-[100dvh]` + safe-area: Telegram режет низ экрана.
9. **Нет второго индикатора прогресса.** Прогресс показывают узлы связки, и только они.
10. **Кислота (`#C8F53C`) не появляется в акте города.** Она — награда лаборатории.
11. **Нет теней без смещения.** Цветное свечение без offset — это декорация, а не глубина.
12. **Нет автоплея со звуком.** Голос стартует только по действию человека.

---

## 3. ЦВЕТ

Снято с референсов. Роли, а не «палитра ради палитры».

### Поверхности

| Токен | Город | Лаборатория | Роль |
|---|---|---|---|
| `--ground` | `#050D1B` | `#0B0A06` | фон приложения |
| `--ground-deep` | `#02060E` | `#050403` | провал, виньетка, подложка модалки |
| `--panel` | `#0C1A2E` | `#16130B` | основная поверхность контента |
| `--raised` | `#162B46` | `#221C10` | приподнятая плашка, поле, чип |
| `--line` | `#243A58` | `#332A18` | границы и разделители |

### Текст

| Токен | Значение | Роль |
|---|---|---|
| `--ink` | `#EDF2FA` | основной текст и заголовки |
| `--ink-2` | `#9DB0CC` (город) / `#B0A489` (лаб) | вторичный текст, подписи |
| `--ink-3` | `#637692` / `#7A6F58` | мета, таймкоды, служебное |
| `--ink-on-gold` | `#0A0F18` | текст на жёлтом |

### Акценты

| Токен | Значение | Роль | Где нельзя |
|---|---|---|---|
| `--gold` | `#F9B706` | главное действие, свет города, бренд | заливать им крупные плоскости |
| `--gold-soft` | `#FFD98A` | свет дальних трасс, hover-подсветка | как цвет текста на тёмном |
| `--electric` | `#3E82F7` | холодные трассы города, ссылки | в акте лаборатории |
| `--acid` | `#C8F53C` | «получено», колба, открытый узел | в акте города |
| `--alert` | `#FF4A3D` | потеря жизни, ошибка теста | как украшение |

**Контраст проверен:** `--ink` на `--ground` = 15.8:1, `--ink-2` на `--panel` = 6.4:1,
`--ink-on-gold` на `--gold` = 12.1:1. Ниже 4.5:1 не опускаемся нигде, кроме
декоративных линий.

---

## 4. ТИПОГРАФИКА

| Роль | Гарнитура | Начертание | Размер / интерлиньяж |
|---|---|---|---|
| Дисплей | Unbounded Variable | 700 | 32–44px / 0.95, tracking −0.03em |
| Заголовок | Unbounded Variable | 600 | 22–26px / 1.1 |
| Текст | Manrope Variable | 400–500 | 16–17px / 1.55 |
| Лейбл | JetBrains Mono Variable | 500, uppercase | 12px / 1.2, tracking 0.14em |
| Данные | JetBrains Mono Variable | 600, tabular | по месту |

**Правила.** Дисплейные заголовки — заглавными, они несут драму сцены. Текст —
строчными, он объясняет. Мера строки 34–46 символов: это телефон, а не десктоп.
Мельче 14px в приложении текста нет вообще.

---

## 5. ПРОСТРАНСТВО И ФОРМА

```
--sp-1: 8px   --sp-2: 12px  --sp-3: 16px  --sp-4: 24px
--sp-5: 32px  --sp-6: 48px  --sp-7: 64px

--r-chip: 8px   --r-panel: 18px   --r-card: 22px   --r-pill: 999px
--gutter: 20px  --app-max: 460px  --bar-h: 84px
```

Одна колонка, ширина ограничена `--app-max`, боковые поля `--gutter` неизменны на
всех экранах. Нижняя панель действия фиксирована и всегда резервирует под собой
место в скролле — контент не заезжает под кнопку.

---

## 6. МОТИОН

Токены: `--e-out: cubic-bezier(0.23, 1, 0.32, 1)`, `--e-inout: cubic-bezier(0.77, 0, 0.175, 1)`,
`--t-press: 140ms`, `--t-ui: 220ms`, `--t-scene: 520ms`.

**Дорогой моушн — ровно в четырёх местах** (§21 прототипа):

1. **Вход в Город** — камера идёт по кадру, трассы разгораются, субтитры набегают.
2. **Открытие Трафик Лаб** — створки расходятся, жёлтый свет заливает кадр.
3. **Допуск получен** — печать, вспышка кислоты, дверь уходит вверх.
4. **Связка собрана** — три узла соединяются линией, кадр выдыхает.

Всё остальное — микро: нажатие `scale(0.97)`, появление `translateY(8px) + opacity`,
стаггер списков 40ms, кроссфейд контента. Ничего не появляется из `scale(0)`.
Выход быстрее входа. Всё прерываемо. При `prefers-reduced-motion` остаются только
прозрачность и цвет — движение выключается, смысл сохраняется.

---

## 6.1 СЛОИ КАДРА

Экран — это кинокадр, а не документ. Слои строго такие:

| Слой | z | Что там |
|---|---|---|
| Сцена | 0 | фотография мира, во всю ширину, медленно дышит |
| Ведущий | 10 | вырезанная фигура, крупная, уходит за нижний и боковой край |
| Текст | 20 | заголовок и субтитры — **поверх сцены и поверх фигуры** |
| Действие | 30–40 | голосовое сообщение и кнопка |

**Текст обязан перекрывать кадр и персонажа — это приём, а не дефект.** Загонять
надписи в «свободные» участки фотографии запрещено: так получается вёрстка, а не кино.
Читаемость держится не отступами, а собственной подложкой блока и тенью текста.

Ведущий на голосовых сценах — не аватарка: высота 50–58vh, он смотрит в камеру и
частично выходит за кадр. Мелкий портрет допустим только в голосовой дорожке и на
экране оффера.

## 7. КОМПОНЕНТЫ

- **Кнопка.** Одна главная на экран, жёлтая заливка, чёрный текст, высота 56px,
  `scale(0.97)` на нажатии. Вторичная — контурная по `--line`, без заливки.
- **Плейсхолдер видео.** Пропорция 16:9, номер протокола, длительность,
  крупная кнопка запуска. Пока видео нет — честная надпись, а не серый прямоугольник.
- **Узел связки.** Три состояния: закрыт (контур, `--ink-3`), открыт (кислота, галка),
  текущий (жёлтая обводка). Это и есть индикатор прогресса.
- **Жизни в тесте.** Пять моно-делений; потеря — деление гаснет и кадр коротко
  отдаёт красным.
- **Голосовое сообщение.** Как в самом Telegram: аватар говорящего, круглая жёлтая
  кнопка play/pause, дорожка формы волны с заливкой по прогрессу, оставшееся время
  моноширинным. Человек сам решает, когда начать; автоплей запрещён. Пока озвучки нет,
  дорожка ведёт сцену по таймингам сценария — play работает всегда.
- **Субтитры.** 24px, полужирные, поверх кадра и фигуры, с собственной подложкой и
  тенью. Появление — снизу с расфокусом, уход быстрее появления.
