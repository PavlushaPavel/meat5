node_modules
dist
.review
.DS_Store
*.local

# Исходники ассетов (кадры заказчика и сгенерированные сцены, ~17 МБ) в репозитории
# не хранятся: в public/world лежат обработанные версии, которыми живёт приложение.
assets-src
*.tsbuildinfo
